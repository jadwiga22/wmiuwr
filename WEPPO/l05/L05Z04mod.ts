export type Person = {
    name : string,
    surname : string,
    age : number
}

export type Worker = {
    name : string,
    surname : string,
    job : string,
    age : number
}

function Foo(n : number) : string {
    return n.toString();
}
